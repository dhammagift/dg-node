---
slug: /login
sidebar_position: 7
---

import AppFrame from '@site/src/components/AppFrame';

# Login

No account or email is required to use Dhamma.gift. "Login" here means
anonymous cloud sync: pick a secret passphrase, and your favorites, history,
reading progress and preferences sync across devices — anyone with the same
passphrase shares the same synced data, so treat it like a password.

<AppFrame src="/login" title="Login / cloud sync" height={500} />

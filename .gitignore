**/nohup.out
assets/audio
nohup.out
result
scripts/api/__pycache__
**/__pycache__
**/dg_db*.json
helpdocs
.well-known
siteroot/config/sync-config.json
siteroot/config/tts-config.json
siteroot/config/*.json
**/node_modules
*.log
package-lock.json
.playwright-cli
# public/assets и public/overrides/js/datatables — junction'ы на C:\soft\dg\assets (см. TODO.md,
# заметка про восстановление настоящего git-symlink для public/assets). Локальный dev-механизм,
# не должен попадать в git как реальный контент.
public/overrides/js/datatables/

# Генерируется сервером при каждом старте (dg-light.js, buildSettingsDemoCache/
# buildLangCountsCache) — не источник, коммитить нечего, каждое окружение строит файлы само.
settings/demo-data.json
settings/lang-counts.json
settings/scripts.json
settings/translator-catalog.json

# Генерируется build-icons.js (npm run build-icons) из @fortawesome/fontawesome-free —
# та же логика, что и dg_db*.json выше: не источник, каждое окружение строит сам.
public/overrides/js/fontawesome-local.js

# Генерируется dg-light.js (buildScriptBundle) при каждом старте сервера — та же логика, что и
# settings/scripts.json выше: не источник, склейка settings.js+dg-text-router.js и
# randPlaceholder.js+home.js для меньшего числа HTTP-запросов на главной.
public/overrides/js/settings-bundle.js
public/overrides/js/home-bundle.js

# Mobile app (offline Android/iOS) — build-offline-db.js output (core.db/lang_*.db, incl.
# SQLite WAL/SHM sidecar files) and native platform folders `npx cap add` generates are build
# artifacts, not source — same "each environment builds its own" logic as dg_db*.json above.
# mobile/www/ is NOT ignored — that's the app's actual source (index.html/app.js/vendor libs),
# analogous to search/ or reader/ above, not a build output.
mobile/dist/
mobile/ios/
# mobile/android/ used to be blanket-ignored here too, back when it was 100% untouched Capacitor
# boilerplate. It now holds real hand-written native source (MainActivity.java's shortcut/
# share-target intent handling, AndroidManifest.xml, res/xml/shortcuts.xml) that would be lost on
# a fresh `npx cap add android` if it weren't tracked — Capacitor's own generated
# mobile/android/.gitignore already correctly excludes build/, .gradle/, local.properties, the
# copied web assets (app/src/main/assets/public), etc., so nothing extra is needed here.
*.db
*.db-wal
*.db-shm

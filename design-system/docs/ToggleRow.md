---
category: Overlays
---

# ToggleRow

A settings switch: label on the left, sliding toggle on the right.

```jsx
<ToggleRow label="Show variants" hotkey="Alt+V" on />
<ToggleRow label="Two columns" hotkey="Alt+C" />
```

When a setting also answers to a keyboard shortcut, spell it out here — otherwise nobody discovers it.

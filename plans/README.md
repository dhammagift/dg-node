# Animation plans

Generated by `improve-animations`. Each plan is self-contained — see the plan file itself for
exact code, values, and verification steps. This index just tracks status and order.

| # | Title | Severity | Category | Status |
| --- | --- | --- | --- | --- |
| 001 | [Home-motto shine: read as a sweep, not a flash](001-home-motto-shine-linear-sweep.md) | MEDIUM | Easing & duration | TODO |

## Execution order

Just the one plan right now — no dependencies. Run it with `improve-animations execute
plans/001-home-motto-shine-linear-sweep.md`, or hand `search/css/home.css:1297-1318` plus the
plan's Target section to any agent.

## Scope note

This is a single targeted plan for the one finding the owner reported (`#home-motto`'s shine
reads as a flash instead of a sweep), not a full-repo audit — invoked as `improve-animations plan
<description>`, which skips the audit phase. No other animation surface in this repo has been
reviewed yet.
